schema_version = 4
name = "EkzD"
protected = [".github/"]
exclude = []
guidance = [
  "Keep EkzD deterministic and project-agnostic; use only the Python standard library.",
  "Preserve exact candidate binding and fail closed when required evidence is unavailable.",
  "Use coherent Conventional Commits at small, meaningful implementation milestones. Keep commits logically focused; avoid batching unrelated changes into one commit and avoid noisy WIP/checkpoint commits.",
  "Retain human merge authority.",
]
